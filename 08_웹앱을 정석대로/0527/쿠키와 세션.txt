* setcookie()함수
- 쿠키정보를 저장하기 위한 함수

* isset()함수
 - 변수값이 설정되어 있는지 판단하고, 설정이 되어 있으면 참, 설정이 안되어 있으면 거짓을 반환(if문과 같음)

* unset()함수
문법 : unset(mixed $var,.....)
- 입력 값으로 주어지는 세션 변수나 변수의 존재를 파괴하고자 할 때 사용.

* session_start() 함수
문법 : bool session_start, 항상 true
세션 초기화
